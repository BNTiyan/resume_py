# Core web framework
flask
flask-cors
gunicorn

# HTTP & scraping
python-docx
rapidfuzz
requests
beautifulsoup4
html5lib
lxml

# Data & documents
PyYAML
reportlab
PyPDF2

# LLM APIs
openai>=1.3.0
google-generativeai>=0.3.1

# Optional & Helper dependencies
langchain-openai
langchain-google-genai
langchain-core
langchain-community
langchain-text-splitters
langchain-huggingface
python-dotenv
loguru

# Free local embeddings + semantic scoring (no API key needed)
# ~80 MB download on first use, then cached
sentence-transformers>=3.0.0

# Heavy dependencies (required for full matching logic)
selenium
webdriver-manager
# playwright is difficult on Heroku without custom buildpacks, keeping commented
# faiss-cpu>=1.9.0.post1 (may cause build issues on Heroku, keeping commented for now)
